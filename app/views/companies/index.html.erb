
<div class="jumbotron">
  <h2>WeGetReview is the best place to find reviews on companies</h2><br>
  <h3>
    Explore the user's favorite company which provide good products & services in your area.<br>
    Leave reviews and rating for your fellow companies.
  </h3><br>
  <button type="button" class="btn btn-danger"><%= link_to "Create Your Free Account Now !!", new_user_registration_path, class: "nav-link" %></button>
</div>

<p id="notice"><%= notice %></p>

<h1>Companies</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td><%= company.address %></td>
        <td><%= company.phone %></td>
        <td><%= link_to company.website, company.website %></td>
        <td><%= link_to 'Show', company, class: "btn btn-info" %></td>
        <td><%= link_to 'Edit', edit_company_path(company), class: "btn btn-success" %></td>
        <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company', new_company_path, class: "btn btn-primary" %>

<!-- Adding Image to the bottom --> 
<%= image_tag "https://s3.eu-west-3.amazonaws.com/wegetreview/Buildings.png" %>
